# Dependencias de Noesis. Fuente de verdad: pyproject.toml (mantener sincronizado).
anthropic>=0.40.0,<1
python-dotenv>=1.0.0,<2
rich>=13.7.0,<15
fastapi>=0.110.0,<1
uvicorn[standard]>=0.29.0,<1
jinja2>=3.1.0,<4
apscheduler>=3.10.0,<4
python-multipart>=0.0.9,<1
itsdangerous>=2.1.0,<3
fpdf2>=2.7.0,<3
# Audio -> texto (Whisper local). OPCIONAL: instálalo para activar las notas de voz
# (necesita más RAM; en Railway conviene subir un poco el plan). Quítale el # cuando
# quieras activarlo, o instálalo aparte con: pip install faster-whisper
# faster-whisper>=1.0.0,<2
