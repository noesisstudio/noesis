{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": [
      "PYTHONPATH=src python -m noesis.migrations upgrade"
    ],
    "startCommand": "python -m uvicorn noesis.web.server:app --host 0.0.0.0 --port $PORT --app-dir src --no-access-log",
    "healthcheckPath": "/ready",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
